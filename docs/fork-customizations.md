# Fork Customizations

**This fork:** [`wltiger/my-cloudcli`](https://github.com/wltiger/my-cloudcli) (`origin` remote). **Upstream:** [`siteboon/claudecodeui`](https://github.com/siteboon/claudecodeui) (`upstream` remote). `main` tracks `upstream/main` as a clean mirror; all work in this fork — including everything below — lives on `dev`. Full remote/branch/sync setup: `docs/agents/issue-tracker.md`. Step-by-step sync procedure: `docs/upstream-sync.md`.

A living inventory of behavior in this fork (`wltiger/my-cloudcli`) that deliberately diverges from upstream (`siteboon/claudecodeui`). When syncing `upstream/main` and a merge conflict (or a suspicious silent behavior change) lands on one of the files listed below, check the matching entry here before deciding whether to keep this fork's version, reapply it on top of upstream's new code, or drop it because upstream solved the same problem natively.

This is not a TODO list — see the repo's issue tracker (`docs/agents/issue-tracker.md`) for open follow-up work. This file only tracks *that a divergence exists and why*.

Entries marked ❌ retired are no longer in the code. They are kept, unrenumbered, so a later reader can see the divergence once existed and why it went away — usually because upstream solved the same problem. Don't reintroduce one without reading its retirement note first.

## 1. Compact mobile sidebar

**Files:** `src/constants/config.ts`, `src/components/sidebar/view/subcomponents/SidebarFooter.tsx`, `SidebarHeader.tsx`, `SidebarContent.tsx`

**Why:** Hides the mobile Report Issue/Discord links and moves the Settings entry point to a tap on the mobile header title, freeing vertical space for the project list.

**On upstream conflict:** Keep mine. Opt-in via `VITE_COMPACT_MOBILE_SIDEBAR` (default off), wraps existing code in conditionals rather than replacing it — low conflict surface. Reapply the conditional branches around whatever upstream changed.

## 2. Sidebar favorites filter

**Files:** `src/components/sidebar/utils/utils.ts`, `hooks/useSidebarController.ts`, `view/Sidebar.tsx`, `view/subcomponents/SidebarContent.tsx`, `SidebarHeader.tsx`, `src/components/settings/hooks/useSettingsController.ts`

**Why:** With enough auto-discovered projects the sidebar gets unwieldy; a star-icon toggle filters the Projects tab down to Starred-only, remembered across sessions.

**On upstream conflict:** Keep mine. Note: the `useSettingsController.ts` change also fixed a real bug (saving settings used to overwrite the whole `claude-settings` blob, silently dropping keys it doesn't own) — that fix is a general correctness improvement, not fork-specific preference; worth proposing upstream separately if convenient.

## 3. Chat reading appearance settings (spacing / font size / width)

**Files:** `src/components/chat/utils/chatSpacing.ts`, `chatTypography.ts`, `hooks/useChatSpacing.ts`, `useChatTypography.ts`, `view/subcomponents/ChatMessagesPane.tsx`, `MessageComponent.tsx`, `ToolGroupContainer.tsx`, `Markdown.tsx`, `ChatComposer.tsx`, `QueuedMessageCard.tsx`, `chat/tools/components/PlanDisplay.tsx`, `ToolErrorDisplay.tsx`, `ContentRenderers/MarkdownContent.tsx`, `QuestionAnswerContent.tsx`, `InteractiveRenderers/AskUserQuestionPanel.tsx`, `src/components/settings/hooks/useSettingsController.ts`, `settings/view/Settings.tsx`, `settings/view/tabs/AppearanceSettingsTab.tsx`, `src/components/quick-settings-panel/view/QuickSettingsContent.tsx`, `ForkQuickSettings.tsx`, `QuickSettingsChatSpacingRow.tsx`, `QuickSettingsChatFontRow.tsx`, `QuickSettingsChatWidthRow.tsx`, `src/i18n/locales/{en,zh-CN}/settings.json`

**Why:** Three knobs over how the chat reads, sharing one storage-plus-event pattern and one pair of surfaces (the Settings modal's Appearance tab and the Quick Settings panel). All three default to upstream's current values, so an untouched install looks exactly like upstream.

- **Message spacing** (Spacious/Compact/None, mobile-only): small phone screens waste space on the fixed horizontal padding around chat bubbles. Because the setting is a no-op above the `sm:` breakpoint, the Quick Settings row keeps a short note saying it only affects narrow screens — rather than hiding the row on desktop, which reads as the setting having disappeared.
- **Reading font size** (Small/Medium/Large/X-Large → `prose-sm`/`prose-base`/`prose-lg`/`prose-xl`, default Small = upstream's 14px): upstream's body text is small on a large monitor, and AskUserQuestion is worse — its inline option *description* is 11px. The level covers **reading text only**: Markdown body (messages, Plan, tool errors, tool markdown results) and AskUserQuestion's question/option/description text. Code blocks, diffs, tool card summary lines, timestamps and badges deliberately keep their sizes — the code editor already has its own `codeEditorFontSize`. AskUserQuestion's existing fullscreen sizes (19/18/15px) stay fixed, and every inline level stays strictly below them so inline can never out-size fullscreen.
- **Content width** (Standard/Wide/Wider/Full, default Standard = upstream's `max-w-[54.25rem]`): with the fixed 288px sidebar, an 868px column uses 38% of a 2560px screen and 28% of a 3440px one. The user bubble's top cap (`xl:max-w-xl`) follows the level too, so a 576px bubble isn't stranded in a 3000px column. Only that top step moves: below `xl` the column hasn't reached its own cap yet, so `sm:max-w-[85%] md:max-w-md lg:max-w-lg` still describes it better than a percentage — a blanket `max-w-[66%]` would actually *shrink* the bubble at `md` (448px column, where it currently fills the width). Each level's value is ~66% of that level's column, which is what `xl:max-w-xl` already worked out to at the standard width, so Standard is byte-identical to before.

Design notes worth keeping: `Markdown.tsx` is the single funnel for the prose level (call sites just drop their `prose-sm`; an explicitly passed size such as fullscreen's `prose-lg` still wins), and CSS `zoom` was rejected as the scaling mechanism because `useComposerMenuAnchor.ts` computes fixed-position menu coordinates from `getBoundingClientRect()` + `window.innerWidth`, which a `zoom` ancestor breaks. Tracked as one customization across all its iterations. Ticket: [#17](https://github.com/wltiger/my-cloudcli/issues/17).

**On upstream conflict:** Keep mine. Pure opt-in settings, additive. In `QuickSettingsContent.tsx` the whole fork footprint is two lines — a `ForkQuickSettings` import and a `<ForkQuickSettings />` appended as the panel body's last child. Every fork-added quick setting lives inside `ForkQuickSettings.tsx` (a file upstream doesn't have), so reapplying after a conflict means restoring those two lines and nothing else; do not push new rows back into upstream's own sections. The column `max-w` is duplicated across `ChatMessagesPane.tsx`, `ChatComposer.tsx` (three sites) and `QueuedMessageCard.tsx` — if upstream changes one of them, all of them must stay in sync or the composer stops lining up with the message list.

⚠️ **i18n policy changed here.** This entry used to state that all strings use `t(key, 'English default')` fallbacks with no `src/i18n/locales/**` edits, keeping translation files a zero-conflict surface. That no longer holds: `en` and `zh-CN` `settings.json` now carry the `appearanceSettings.chatSpacing` / `chatFontSize` / `chatWidth` keys, so the Chinese UI is complete. A fork-only i18n namespace was considered as a zero-conflict alternative and rejected — `src/i18n/config.js` hardcodes every namespace's import, `resources` entry and `ns` entry, making a new namespace a *larger* conflict surface than additive JSON keys. On conflict in those two files, keep both sides' keys; the other nine locales are still untouched and fall back to the inline English defaults.

## 4. Model label prettification + wider selector

**Files:** `src/components/chat/view/subcomponents/ComposerModelMenu.tsx`, `chat/utils/modelLabel.ts` (+ test)

**Why:** A model set via the Claude Code CLI directly (not CloudCLI's own switcher) showed a raw, sometimes-truncated internal model id instead of a readable name.

**On upstream conflict:** Keep mine, but re-read `docs/adr/0001-strict-model-label-fallback.md` first if upstream ships something in this area — it specifically rejected fuzzy/substring matching in favor of strict-pattern-or-raw-fallback; compare approaches rather than assuming mine still wins.

## 5. Offline-capable app shell (service worker cache-first) ⚠️ re-evaluate, don't blindly keep

**Files:** `public/sw.js`, `src/components/settings/view/tabs/AboutTab.tsx`

**Why:** CloudCLI ships PWA infrastructure but didn't actually work offline — the service worker deliberately never cached the HTML/JS shell, and the server sends `no-cache` on HTML specifically "to prevent service worker issues." This fork reverses that decision: cache-first app shell + a manual "Refresh offline cache" control, no automatic staleness detection.

**On upstream conflict:** **Highest-priority re-check in this list.** This directly reverses an intentional upstream design choice. If upstream ever touches `public/sw.js` or the `Cache-Control` headers in `server/index.ts`, re-read `docs/adr/0002-cache-first-app-shell-manual-refresh.md` before reapplying — upstream may have addressed the exact staleness failure mode that made them avoid this in the first place, which would change the tradeoff.

## 6. PWA / title name unified to "CloudCLI"

**Files:** `index.html`, `public/manifest.json`, `src/utils/pageTitleNotification.ts`, `src/components/sidebar/view/subcomponents/SidebarProjectList.tsx`

**Why:** Three different strings existed for the app name ("CloudCLI UI" in the manifest/title, "Claude UI" in the iOS meta tag) — unified to "CloudCLI", the product's current name.

**On upstream conflict:** Keep mine, but check first — if upstream also unifies its own naming, this customization may become fully redundant and can just be dropped.

## 7. Remember last session on PWA reopen

**Files:** `src/hooks/useDeviceSettings.ts`, `src/hooks/useProjectsState.ts`

**Why:** An installed PWA always launches into `manifest.json`'s `start_url` ("/"), landing on the empty project picker instead of wherever the user left off.

**On upstream conflict:** Keep mine. Opt-in via `display-mode: standalone` detection — a plain browser tab is unaffected.

## 8. Version display: build git SHA + timestamp

**Files:** `vite.config.js`, `src/vite-env.d.ts`, `src/components/settings/view/tabs/AboutTab.tsx`, `server/index.ts`

**Why:** With the offline cache-first shell (#5) it became hard to tell whether a running instance is a fresh build or a stale cached one. Shows the build's git SHA + timestamp next to the version badge, front and back.

**On upstream conflict:** Keep mine. Deliberately never touches `package.json`'s `version` field (kept identical to upstream, which owns that field) — designed specifically to have near-zero conflict surface.

## 9. `.claude/` tracked selectively (CLAUDE.md + skills/)

**Files:** `.gitignore`

**Why:** Upstream ignores `.claude/` entirely. This fork carves out exceptions for `.claude/CLAUDE.md` (this repo's Claude Code guidance — see this file's own intro for why it lives there instead of a root `CLAUDE.md`, which upstream's `.gitignore` also excludes) and `.claude/skills/` (project-scoped Claude Code skills, e.g. `sync-upstream`), so both travel with the fork instead of being local-only. Everything else under `.claude/` (`settings.local.json`, etc.) stays ignored — genuinely personal/local state.

**On upstream conflict:** Keep mine. If upstream adds new patterns to the same `# AI specific` block, reapply the two negation lines (`!.claude/CLAUDE.md`, `!.claude/skills/` + `!.claude/skills/**`) *after* every pattern that could match those paths — gitignore re-inclusion only wins over rules earlier in the file, and the existing bare `CLAUDE.md` rule matches `.claude/CLAUDE.md` too if the negation isn't placed after it.

## 10. Web Push VAPID subject that APNs accepts

**Files:** `server/modules/notifications/vapid-keys.service.ts`

**Why:** Upstream hardcodes `mailto:noreply@claudecodeui.local` as the VAPID `sub` claim. `.local` is a reserved TLD, and APNs validates that claim — it rejects every push with `403 BadJwtToken`, so **no iOS device has ever received a notification**. Verified against a live install with a real Home Screen web app subscription: same subscription, same keys, only the subject changed — `.local` → 403, `https://cloudcli.ai` → 201, a real `mailto:` → 201. Chrome's FCM and Mozilla's Autopush don't validate the domain, which is why the breakage is iOS-only and went unnoticed upstream.

**On upstream conflict:** Take upstream's version if they've fixed it themselves — any routable `mailto:`/`https:` value works, the specific string doesn't matter. Otherwise keep mine and reapply. This is a plain bug fix rather than a fork preference, so it's worth proposing upstream as a PR; once upstream ships an equivalent fix this entry can be dropped.

## 11. Fullscreen reading for chat messages and AskUserQuestion

**Files:** `src/shared/view/ui/FullscreenSurface.tsx` (new), `src/shared/view/ui/index.ts`, `src/components/chat/view/subcomponents/MessageFullscreenControl.tsx` (new), `src/components/chat/view/subcomponents/MessageComponent.tsx`, `src/components/chat/tools/components/InteractiveRenderers/AskUserQuestionPanel.tsx`, `src/i18n/locales/*/chat.json`

**Why:** On a phone the chat column is too narrow to read a long assistant message, and AskUserQuestion's option labels + descriptions get squeezed until you cannot tell the options apart — which defeats the point, since answering those prompts is exactly what you do away from the desk. One shared `FullscreenSurface` (portaled `inset-0` panel, Escape to close, body scroll lock, safe-area padding) backs both: a maximize button next to the message copy control, and one in the AskUserQuestion header (which also relaxes its `max-h-48` option list and bumps text sizes while expanded). Shown on desktop too — same code path, no breakpoint branch. Answering auto-exits fullscreen; the state is never persisted. The surface also carries the copy control in its header, a swipe-down-to-dismiss gesture (armed only while the body is scrolled to the top, so it never fights scrolling), and a `max-w-3xl` measure so lines stay scannable on a wide desktop. AskUserQuestion additionally pins its Skip/Back/Submit bar to the bottom of the viewport (via a `footer` slot on the surface — a `sticky` footer would be trapped by the card's own `overflow-hidden`) and titles the surface with the session name, threaded down as an optional `sessionTitle` on `PermissionPanelProps`.

**On upstream conflict:** Keep mine. `FullscreenSurface` is a new file with no upstream counterpart; the touch points in `MessageComponent.tsx` (one import + one entry in the footer control row) and `AskUserQuestionPanel.tsx` (state, header button, the `panel` variable + wrapper at the bottom, the `isFullscreen ?` class ternaries) are small and easy to reapply if upstream reworks either file.

## 12. Touch-reachable file tree menu + copy relative path

**Files:** `src/components/file-tree/view/FileContextMenu.tsx`, `FileTreeNode.tsx`, `FileTreeList.tsx`, `FileTreeBody.tsx`, `FileTree.tsx`, `src/components/file-tree/hooks/useFileTreeOperations.ts`, `utils/fileTreeUtils.ts`, `src/i18n/locales/*/common.json`

**Why:** Upstream mounts the file tree context menu on `onContextMenu` alone, so on a touch device the *entire* menu is unreachable — rename, delete, download, new file/folder, copy path. Two entry points are implemented on purpose so they can be compared on a real phone before deciding whether to drop one: a 500 ms long press (abandoned once the finger drifts >10 px, so scrolling still works) and a per-row `⋮` button rendered only below `md:`. Both drive the same menu state; `FileContextMenu` now also accepts a function child so a row can render its own trigger without owning that state. In detailed view the `⋮` takes over the permissions column on mobile, which was already too narrow to show `rw-rw-rw-` beside a button. Separately, a "Copy Relative Path" entry sits next to upstream's absolute "Copy Path" (two flat entries, no submenu), and both now go through the shared `copyTextToClipboard` util — upstream called `navigator.clipboard` directly and fired the success toast synchronously, so a failed copy showed "Failed to copy path" *and* "Path copied to clipboard".

**On upstream conflict:** Keep mine, but re-check the entry points first — if upstream has since added its own touch affordance, take theirs and drop whichever of the two duplicates it. The clipboard fix and the double-toast fix are plain bug fixes rather than fork preferences and are worth proposing upstream. Note this entry does edit `src/i18n/locales/*/common.json` (two keys inside upstream's existing `fileTree.context` block), unlike entry 3's deliberate `t(key, 'English default')`-only approach — the block is upstream's, so expect a small conflict there and reapply the two added keys.

## 13. Cross-project session switcher reachable from the chat title ❌ retired (v1.37.1)

**Files:** `src/lib/commandPaletteEvents.ts`, `src/components/command-palette/ForkRecentSessions.tsx`, `CommandPalette.tsx`, `src/components/main-content/view/subcomponents/MainContentTitle.tsx`, `src/utils/api.js`, `server/modules/providers/provider.routes.ts`, `services/sessions.service.ts`, `server/modules/database/repositories/sessions.db.ts`

**Why:** Upstream's command palette can already switch sessions, but its only trigger is Cmd/Ctrl+K — the sidebar's palette affordance is a `pointer-events-none` `<kbd>` badge hidden below `md:`. So on a phone the palette is unreachable and switching sessions means opening the sidebar. Tapping the chat title now opens the palette straight on its Sessions page, which also unlocks the palette's file/commit/branch search on touch devices. Upstream's Sessions group is scoped to the selected project (the same set the sidebar shows), so a fork-owned "Recent sessions (all projects)" group answers "what was I just in" regardless of project; it is deduped against the rows upstream already listed. Switching is just `navigate('/session/:id')` — the existing session resolver picks up the owning project, so cross-project needs no extra project-switch wiring. The backend gained `getRecentSessions(limit)` (`getAllSessions` is unordered and unbounded) plus `GET /api/providers/sessions/recent`; `sessions/running` could not be reused because it is deliberately status-only.

**Retired (v1.37.1):** Upstream shipped a native cross-project recent-conversation feed in the sidebar (PR #1041), including its own paginated `GET /api/providers/sessions/recent`. About 70% of this entry's backend was redundant with it, and the overlap was not benign: both route registrations survived the merge on the same path, and Express matches the first, so upstream's silently shadowed this one. What stayed genuinely unique was the mobile entry point — tapping the chat title to reach the palette — and that alone was judged not worth carrying as ongoing divergence, so the customization was dropped whole rather than adapted onto upstream's feed. Removed in the v1.37.1 sync: `src/lib/commandPaletteEvents.ts` and `src/components/command-palette/ForkRecentSessions.tsx` deleted; the palette's event listener and extra group gone; the chat title back to plain text. If the missing mobile entry point turns out to hurt, reintroduce only that — a trigger for upstream's palette, not a second session list — as a new entry.

## 14. Browser runtime installs into a managed directory

**Files:** `server/modules/browser-use/browser-use.service.ts`

**Why:** Upstream's `installRuntime()` runs `npm install --no-save --no-package-lock playwright` with `cwd: process.cwd()`, but `getPlaywright()` resolves `require('playwright')` from the module's own location. For a globally installed CloudCLI those are two different trees: the install lands in whatever directory the user happened to launch the CLI from (often the home directory), which node never walks into when resolving from `<npm root -g>/@cloudcli-ai/cloudcli/dist-server/server/modules/browser-use/`. So the install genuinely succeeds, the Settings tab still reports `Playwright: missing`, and the "Install Runtime" button looks like it does nothing no matter how often it is clicked — each click just reinstalls into the same unreachable place. Installing now targets `~/.cloudcli/browser-use/runtime` (beside the existing `profiles/` dir), seeded with a private `package.json` so npm does not walk up and adopt whatever project sits above it, and `getPlaywright()` tries the module's own resolution first and that directory second. `runCommand()` took a `cwd` argument for this. Dropping `--no-save --no-package-lock` is safe now that the manifest is fork-owned, and keeping the lockfile makes reinstalls cheaper.

**On upstream conflict:** This is a plain bug fix rather than a fork preference — worth proposing upstream, and drop this entry if they take it. Until then keep mine, but treat `installRuntime()` and `getPlaywright()` as a pair: they only work together, so if upstream rewrites either one, reapply both halves rather than merging one side and leaving the other. If upstream instead solves it by shipping playwright as a real dependency, drop this entirely — the first resolver already covers that case.

## 15. Tab switcher collapses into a menu on mobile

**Files:** `src/components/main-content/view/subcomponents/MainContentTabMenu.tsx`, `MainContentTabSwitcher.tsx`, `MainContentHeader.tsx`, `src/shared/view/ui/ActionMenu.tsx`

**Why:** Upstream puts the header title and the tab pills on one row. Below `lg:` the pills are already icon-only, but the count is not fixed — 4 built-ins plus optional Browser and Tasks plus one per enabled plugin — so on a 375px phone the strip takes ~148–220px and the title is left with under 150px (measured 51px with 6 tabs). Upstream's own mitigation, a horizontal scroller with gradient fade masks, stops the overflow but hands the title no width back. That costs more in this fork than upstream because entry 13 turned the chat title into the cross-project session switcher, so squeezing it also squeezes a navigation entry point. Below 768px — `isMobile`, the same flag the header already uses for the hamburger — the whole strip now collapses into one ~48px pill showing the active tab's icon plus a chevron; tapping it opens a dropdown listing every tab in the current order, divider before the plugin group, active row highlighted and marked `aria-current`. Measured title width went 51px → 217px. Desktop is untouched, scroller and fade masks included.

**On upstream conflict:** Keep mine. The upstream footprint is deliberately tiny and purely additive: one prop plus one early `return` in `MainContentTabSwitcher.tsx`, one prop pass-through in `MainContentHeader.tsx`, and four optional props on `ActionMenu` (`triggerIcon`, `showChevron`, item `iconNode`, item `isActive`) that its two existing call sites never set. All the menu code lives in the fork-owned `MainContentTabMenu.tsx`, so reapplying means restoring those spots. The early `return` sits *after* the tab list is built on purpose — both renderings share one list, so a built-in tab added upstream shows up in the mobile menu for free; keep that ordering when reapplying. The dropdown uses `ActionMenu`'s `portal` mode deliberately: the header's tab slot is `overflow-hidden` and would clip an absolutely-positioned menu, so if upstream restructures that wrapper, re-check the clipping before switching away from portal. Strings use `t(key, { defaultValue })`, no `src/i18n/locales/**` edits (same as entries 3 and 13). If upstream ships its own mobile tab affordance, take theirs and drop this.

## 16. Push notifications collapse to one-per-session, cleared on open or once resolved elsewhere

**Files:** `server/modules/notifications/services/notification-orchestrator.service.js`, `server/modules/notifications/index.ts`, `server/modules/providers/list/claude/claude-runtime.provider.js`, `public/sw.js`, `src/components/app/AppContent.tsx`

**Why:** Upstream tags each push `provider:sessionId:code`, so a session that fires a permission request, then a stop, then an error keeps three separate notifications stacked in the phone's notification center forever — none of them ever gets cleared, including the ones you already acted on inside the app. This fork drops `code` from the tag when a `sessionId` is present (`provider:sessionId`). The obvious mechanism — the OS replacing same-tag notifications on its own (`renotify: true`) — turned out to only work on Chrome/Android: iOS Safari never replaces same-tag notifications (verified against a real device; matches [WebKit bug 258922](https://bugs.webkit.org/show_bug.cgi?id=258922)), so `sw.js`'s `push` handler now closes any already-shown notification for the same `sessionId` itself (`closeSessionNotifications()`) before calling `showNotification()`, rather than relying on the browser to do it. Opening that session in the app additionally posts `{ type: 'CLEAR_SESSION_NOTIFICATIONS', sessionId }` to the service worker, reusing the same close helper — this is what catches the case where the tag/session-collapse path was bypassed. Sessionless events (e.g. `push.enabled`) keep a `provider:global:code` tag so they don't collapse into each other. Separately: answering a permission request (including AskUserQuestion) on one device left the same request's push notification stuck on every *other* subscribed device, since nothing told them it had been resolved. `chat.permission-response` already resolves through provider-neutral `resolveToolApproval()` regardless of which client sent it, so that function now also fires `notifyPermissionResolved()` — a real, spec-compliant push (not a silent/data-only one, which risks the browser treating the subscription as abusive and revoking it) with code `permission.resolved` reusing the same session tag, which collapses the stale "action required" notification into a "handled" one everywhere via the mechanism above. The resolver metadata gained `_userId`/`_sessionName` (alongside the existing `_sessionId`/`_toolName`) so `resolveToolApproval()` has what it needs without threading extra parameters through the whole call chain.

**On upstream conflict:** Keep mine. Small, self-contained footprint: one tag expression + one `closeSessionNotifications()` helper + one `permission.resolved` notify function in the orchestrator/`sw.js`, a few extra resolver metadata fields plus one `notifyPermissionResolved()` call in `claude-runtime.provider.js`'s `resolveToolApproval()`, one `useEffect` in `AppContent.tsx` keyed on the route's `sessionId`. If upstream reworks the push payload/tag scheme or the permission-approval flow, keep the "one notification per session, cleared on open or once resolved anywhere" behavior and re-derive the tag/clear/resolve logic against their new shape rather than dropping the feature. If upstream ever adds interactive approvals to another provider, that provider needs the same `_userId`/`_sessionName` metadata and `notifyPermissionResolved()` call in its own `resolveToolApproval`-equivalent — this was only wired into Claude's because it's the only provider with `permissions.resolve` today.

## 17. Codex model catalog fetched live instead of a stale snapshot ❌ retired (v1.37.1)

**Files:** `server/modules/providers/list/codex/codex-models.provider.ts`, `server/modules/providers/services/provider-models.service.ts`

**Why:** Upstream's `getSupportedModels()` only ever read `~/.codex/models_cache.json`, a point-in-time snapshot nothing refreshes. A user routing Codex through a custom `model_provider`/relay (declared via `model_catalog_json1` in `~/.codex/config.toml`) or simply sitting on an older cache never saw newly-released official models — verified: GPT-5.6 Sol/Terra/Luna, publicly released 2026-07-09, were absent from a cache last fetched 2026-06-21. The `@openai/codex-sdk` JS API has no method for this; the equivalent only exists as the bundled `codex` binary's own `debug models` subcommand. This fork now spawns that subcommand (via the same `@openai/codex` binary `codex-runtime.provider.js` already uses, resolved through its `package.json` `bin` field so both stay on the same version) and parses its JSON output, falling back to the old cache-file read and then the static `CODEX_FALLBACK_MODELS` list on any failure. `codex` was also added to `provider-models.service.ts`'s `UNCACHED_PROVIDERS` — the outer 3-day disk-persisted cache would otherwise keep shadowing the fresh subprocess call, since a successful result there is memoized well past the point a new model ships.

**Retired (v1.37.1):** The gap that motivated this closed on its own — upstream refreshed its own static Codex catalog in the same release, so the newly-released models this entry existed to surface now ship in the shipped list. Keeping the live fetch would then have cost more than it gained: it *replaced* the catalog rather than merging into it, so any option upstream added that `codex debug models` does not report would have silently vanished from the picker — a regression, in the exact area this entry was supposed to protect. Removed in the v1.37.1 sync: `codex-models.provider.ts` and `provider-models.service.ts` taken from upstream wholesale, so `fetchLiveCodexModels()`, the `codex debug models` subprocess, and `UNCACHED_PROVIDERS` are all gone. If the catalog goes stale again, whatever replaces this has to *merge with* upstream's list rather than replace it, and should prefer a real SDK method over the undocumented `debug` subcommand.

## 18. Claude model catalog fetched live via SDK instead of a hardcoded list ❌ retired (v1.37.1)

**Files:** `server/modules/providers/list/claude/claude-models.provider.ts`

**Why:** `getSupportedModels()` had a real SDK call for this already written, but commented out: calling `query()` to get a `Query` instance persists a session jsonl under `~/.claude/projects/`, which the sidebar's own project-discovery then picks up as a stray workspace. `@anthropic-ai/claude-agent-sdk` (already at latest, 0.3.227) has since grown `persistSession: false`, an option documented specifically for "ephemeral... workflows where session history is not needed." Verified empirically twice — the raw SDK call, then the real `ClaudeProviderModels` class — by diffing `~/.claude/projects/` before and after: no new session either time, ~2.4–3.3s round trip. The live catalog is genuinely different from the hardcoded fallback, not just fresher: it is missing a standalone "Opus" and "Sonnet[1m]" the old list had, and adds a `resolvedModel` field plus a richer effort-levels shape.

**Retired (v1.37.1):** Same reasoning as #17. Upstream's static list gained options the live SDK fetch does not return — `best`, `opusplan`, and an `xhigh` effort tier — so keeping the fetch would have removed model choices a user can now pick. Removed in the v1.37.1 sync: `claude-models.provider.ts` taken from upstream wholesale, so `fetchLiveClaudeModels()`, `mapClaudeModel()`, `buildClaudeModelsDefinition()`, and the `persistSession: false` SDK call are gone; `CLAUDE_FALLBACK_MODELS` no longer exists at all, upstream having renamed the static list `CLAUDE_PREDEFINED_MODELS`. Don't revive the `persistSession: false` idea. Before this fork found it independently, another contributor proposed the identical mechanism upstream (`persistSession: false`, the same `pathToClaudeCodeExecutable` resolution, fallback-on-failure, alias retention) in [siteboon/claudecodeui#1132](https://github.com/siteboon/claudecodeui/pull/1132) — closed unmerged, with a maintainer's one-line reply: "this is intentional." Read alongside the same release gutting Cursor's and OpenCode's model providers to the identical static-catalog pattern, that reads as a deliberate cross-provider policy against any runtime/provider-native model discovery, not a verdict on that one PR's implementation quality. Respect it — don't re-propose the same mechanism upstream, and don't rebuild it here either.

## 19. New Session picker only lists connected providers

**Files:** `src/components/chat/view/subcomponents/ProviderSelectionEmptyState.tsx`

**Why:** Upstream always lists all four providers (claude/cursor/codex/opencode) in the New Session model picker regardless of whether that provider is actually installed/authenticated — picking an unconnected one only fails later, when a session tries to actually run. This fork wires in the existing `useProviderAuthStatus` hook (already used by Settings → Agents) and filters the picker down to providers whose `/auth/status` reports `authenticated`. All four still show while that check is in flight, to avoid a flash of four narrowing to two.

**On upstream conflict:** Keep mine — a genuine UX preference, not a bug fix (upstream may deliberately want unconnected providers visible/discoverable, e.g. for onboarding). If upstream reworks this file, reapply the `useProviderAuthStatus()` call, the mount `useEffect` that triggers `refreshProviderAuthStatuses()`, and the `connectedProviders`/`isCheckingConnections` filter feeding `visibleProviderGroups`.

## 20. Scheduled session triggers (send a message later)

**Files:** `server/modules/database/schema.ts`, `server/modules/database/index.ts`, `server/modules/database/repositories/scheduled-triggers.db.ts`, `server/modules/scheduled-triggers/` (new module: `index.ts`, `scheduled-triggers.routes.ts`, `services/scheduled-trigger.service.ts`, `services/scheduler-poller.service.ts`, `tests/`), `server/index.ts`, `server/modules/notifications/services/notification-orchestrator.service.js`, `.env.example`, `src/utils/api.js`, `src/components/chat/hooks/useScheduledTrigger.ts`, `src/components/chat/view/subcomponents/ComposerScheduleMenu.tsx`, `ChatComposer.tsx`, `src/components/chat/view/ChatInterface.tsx`, `src/i18n/locales/en/chat.json`

**Why:** Some Codex/Claude relays (third-party API resellers) enforce their own rate-limit windows and return a plain-text error naming a clock time to retry after (e.g. `请在 今天 15:00 后再试`) instead of a structured `retry-after`. Upstream has no notion of "wait, then resend" at all — a rate-limited session just sits there until the user manually comes back and retypes. This adds a generic, provider-agnostic "schedule a message" primitive: any session can queue a one-shot future message (defaulting to `continue`, editable) via a new clock-icon composer menu; a 30s backend poller fires it through the same `providerRuntimeService.run()` seam `chat.send` and the headless `/api/agent` route already share, so no provider gets special-cased.

The poller claims a trigger (`pending` → `firing`) *before* calling the provider, not after — the first version marked it fired only on success, so a provider call slower than one poll interval got re-picked-up and fired again by the next tick (observed firsthand: a slow response sent the same `"continue"` into a live session repeatedly). A trigger stuck `firing` from a crash mid-fire is reset to `pending` on the next boot so it isn't lost forever, just retried.

The composer UI is time-only — no date picker at all. A bare `HH:mm` always means "the next occurrence of that clock time": today if it hasn't passed yet, tomorrow (at most 24h out) otherwise — computed fresh on every render, not decided once and stored. An earlier version *did* store that decision on the input's `onChange`, and a native time input fires `onChange` once per segment as you type (hour, then minute) rather than once when you're done; a not-yet-finished intermediate value could permanently latch onto "tomorrow" mid-edit even though the value you finished typing was still later today. A plain, non-interactive "Tomorrow" label appears next to the input when applicable; there's deliberately no way to pick an arbitrary date. Five presets cover the common cases: `+10min`/`+60min` (stack — each click adds to whatever's currently drafted, not to "now"), the next on-the-hour mark, the next `{0,3,6,9,12,15,18,21}` mark, and a fixed `00:05` "first thing tomorrow" shortcut. Reopening the popover always resets to a fresh default rather than whatever was left over from a previous open/schedule/cancel.

Deliberately scoped to a manual, one-shot primitive for this pass — automatically detecting a relay's rate-limit text and auto-creating a trigger from it is an intentionally deferred follow-up, not built here. Fires headlessly (no live WebSocket client attached), so an already-open chat tab does not see the reply stream in — it only learns the session changed via a `session_upserted` ping, which the frontend already treats as sidebar-only; genuinely live-streaming a headless-fired run into an open tab is unaddressed. Missed triggers (server down at `trigger_at`) fire late within a configurable grace window (`SCHEDULED_TRIGGER_GRACE_WINDOW_MINUTES`, default 60) and are marked `expired` beyond it, rather than either silently dropping or firing an arbitrarily stale message.

**On upstream conflict:** Keep mine. New table + new module, near-zero conflict surface on its own; the only edits to files upstream also owns are additive (one `CREATE TABLE IF NOT EXISTS` block + two indexes appended to `INIT_SCHEMA_SQL` in `schema.ts`, one repository export line in `database/index.ts`, two route-mount/poller-start lines plus one import in `server/index.ts`, one `ComposerScheduleMenu` slot in `ChatComposer.tsx` next to the existing `ComposerPermissionMenu`, one `sessionId` prop threaded from `ChatInterface.tsx`, two new `CODE_MAP` entries in the notification orchestrator). If upstream touches `chat-websocket.service.ts`'s `runtimeOptions` construction (`cwd`/`projectPath` from the session row), mirror the same shape in `scheduled-trigger.service.ts`'s `fireTrigger()` — it was deliberately copied from there rather than factored into a shared helper. If upstream ever adds a real "resend into an open tab" push mechanism, reuse it here instead of the current `session_upserted`-only ping.
